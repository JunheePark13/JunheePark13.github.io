name: Unlock Pages concurrency

on:
  push:
    branches: ["main"]

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  unlock:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Clearing stale Pages concurrency lock"
